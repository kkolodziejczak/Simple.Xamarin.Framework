<?xml version="1.0" encoding="UTF-8"?>
<Frame xmlns="http://xamarin.com/schemas/2014/forms" 
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:Simple.Xamarin.Framework"
       x:Class="Simple.Xamarin.Framework.ExtendedButton"
       Padding="30,5"
       Margin="0"
       HorizontalOptions="Start"
       OutlineColor="Transparent"
       HasShadow="True"
       >
    <Frame.Triggers>
        <Trigger TargetType="Frame" Property="IsEnabled" Value="True">
            <Setter Property="BackgroundColor" Value="{Binding LocalBackgroundColor}"/>
        </Trigger>
        <Trigger TargetType="Frame" Property="IsEnabled" Value="False">
            <Setter Property="BackgroundColor" Value="DarkGray"/>
        </Trigger>
    </Frame.Triggers>
    <Frame.GestureRecognizers>
        <TapGestureRecognizer Command="{Binding TapCommand}" NumberOfTapsRequired="1"/>
    </Frame.GestureRecognizers>
    <local:ExtendedLabel Text="Test"/>
</Frame>